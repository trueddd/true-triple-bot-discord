ktor {
    deployment {
        environment = development
        port = 8080
        port = ${?PORT}
        watch = [ movie-chooser ]
    }
    application {
        modules = [ MainKt.module ]
    }
}
